import Location from './location';

export default class Box {
    id: number;
    location: number;

    constructor(
    id: number,
    location: number,
    ) {
    this.id = id;
    this.location = location;
    }
}
