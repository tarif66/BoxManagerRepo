export default class User {
    id: number;
    forName: string;
    lastName: string

    constructor(
    id: number,
    forName: string,
    lastName: string

    ) {
    this.id = id;
    this.forName = forName;
    this.lastName = lastName;
    }
}
    